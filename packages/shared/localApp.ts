/** Public HTTP port for `pnpm dev` / `pnpm start`. NYSE founding year (Buttonwood, 1792-05-17). */
export const LOCAL_APP_PORT = 1792;
export const LOCAL_APP_ORIGIN = `http://localhost:${LOCAL_APP_PORT}`;
