import * as stylex from '@stylexjs/stylex';
import { colors } from '../../theme/tokens.stylex';

export const timelineState = stylex.defineVars({
  dayColor: colors.textSecondary,
  dotBorderColor: colors.borderStrong,
});
