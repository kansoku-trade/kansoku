---
import features from '../data/features.json';
import Base from '../layouts/Base.astro';
import { subscription } from '../lib/pricing';
import { latestDesktop } from '../lib/releases';

const latest = await latestDesktop();
const version = latest.version;

const monthlyAmount = subscription.priceLabel.split(' / ')[0];
const monthlyListAmount = subscription.listPriceLabel.split(' / ')[0];
const yearlyAmount = subscription.yearly.priceLabel.split(' / ')[0];
const yearlyListAmount = subscription.yearly.listPriceLabel.split(' / ')[0];

const matrix = [
  ...features.matrixFree.map((section) => ({
    group: section.group,
    rows: section.items.map((cap) => ({ cap, free: true as const })),
  })),
  ...features.matrixCompare,
  {
    group: 'Pro 能力',
    rows: features.pro
      .filter((f): f is typeof f & { matrix: string } => Boolean(f.matrix))
      .map((f) => ({ cap: f.matrix, free: false as const })),
  },
];

const faq = [
  {
    q: '订阅后怎么激活？',
    a: '付款完成后收到 license key，在应用「设置 → 授权」里粘贴激活，绑定你自己的设备。',
  },
  {
    q: '试用需要绑卡吗？',
    a: `走 Dodo Payments 的订阅流程，${subscription.trialDays} 天试用期内取消不收费。`,
  },
  {
    q: '不订阅会失去什么？',
    a: `只是 ${features.pro.length} 项 Pro 自动化能力不可用；看盘本体和免费 AI 永久免费，数据始终在你本机。`,
  },
];
---

<Base
  title="定价 — Kansoku"
  description={`Kansoku 免费版即完整看盘工具；Pro 限时 5 折 ${subscription.priceLabel}（原价 ${subscription.listPriceLabel}），年付 ${subscription.yearly.priceLabel}，${subscription.trialDays} 天免费试用。`}
>
  <section class="head">
    <div class="eyebrow">two tickets · one desk</div>
    <h1>定价</h1>
    <p>像下单一样清楚：一张免费，一张付费。</p>
  </section>

  <section class="desk">
    <div class="ticket">
      <div class="t-head"><span>order · free</span><span>#0000</span></div>
      <div class="t-body">
        {
          features.freeTicket.map((item) => (
            <div class="t-row">
              <span class="k">{item}</span>
              <span class="v up">included</span>
            </div>
          ))
        }
        <div class="t-row"><span class="k">license</span><span class="v">agpl-3.0 + cc</span></div>
      </div>
      <div class="t-total"><span>total</span><b>$0</b></div>
      <div class="t-cta">
        <a class="btn btn-ghost" href={latest.downloadUrl}>↓ 下载 v{version}</a>
      </div>
    </div>

    <div class="ticket ticket--pro">
      <div class="t-head"><span>order · pro</span><span>#0001</span></div>
      <div class="t-body">
        <div class="t-row"><span class="k">free 全部</span><span class="v up">included</span></div>
        {
          features.pro.map((f) => (
            <div class="t-row">
              <span class="k">{f.ticket}</span>
              <span class="v up">+ added</span>
            </div>
          ))
        }
        <div class="t-row">
          <span class="k">trial</span><span class="v">{subscription.trialDays} days · $0</span>
        </div>
      </div>
      <div class="t-total">
        <span>total</span>
        <div class="t-price">
          <span class="t-badge">{subscription.discountLabel}</span>
          <div class="t-price-row">
            <s class="t-list">{monthlyListAmount}</s>
            <b>{monthlyAmount}<small>/月</small></b>
          </div>
        </div>
      </div>
      <div class="t-cta">
        <a class="btn" href={subscription.url} target="_blank" rel="noreferrer"
          >免费试用 {subscription.trialDays} 天</a
        >
        <p class="t-alt">
          或 <a href={subscription.yearly.url} target="_blank" rel="noreferrer">
            <s class="t-list-inline">{yearlyListAmount}</s>
            {yearlyAmount}/年（{subscription.yearly.discountLabel} · {subscription.yearly.savingsLabel}）
          </a>
        </p>
      </div>
    </div>
  </section>

  <section class="matrix">
    <div class="rule">full breakdown</div>
    <div class="table-wrap">
      <table>
        <thead>
          <tr><th></th><th class="col">Free</th><th class="col pro">Pro</th></tr>
        </thead>
        <tbody>
          {
            matrix.map((section) => (
              <>
                <tr class="group">
                  <td colspan="3">{section.group}</td>
                </tr>
                {section.rows.map((row) => {
                  const freeLabel = typeof row.free === 'string' ? row.free : row.free ? '✓' : '—';
                  const proLabel = 'pro' in row && typeof row.pro === 'string' ? row.pro : '✓';
                  const freeMark = typeof row.free === 'string' ? 'mark text' : row.free ? 'mark yes' : 'mark no';
                  const proMark =
                    'pro' in row && typeof row.pro === 'string' ? 'mark text pro-col' : 'mark yes pro-col';
                  return (
                    <tr>
                      <td class="cap">{row.cap}</td>
                      <td class={freeMark}>{freeLabel}</td>
                      <td class={proMark}>{proLabel}</td>
                    </tr>
                  );
                })}
              </>
            ))
          }
        </tbody>
      </table>
    </div>
  </section>

  <section class="faq">
    <div class="rule">faq</div>
    <dl>
      {
        faq.map((item) => (
          <>
            <dt>{item.q}</dt>
            <dd>{item.a}</dd>
          </>
        ))
      }
    </dl>
  </section>
</Base>

<style>
  .head {
    max-width: 1120px;
    margin: 0 auto;
    text-align: center;
    padding: 72px 24px 8px;
  }

  .eyebrow {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 20px;
  }

  .head h1 {
    font-size: 44px;
    margin: 0 0 12px;
  }

  .head p {
    color: var(--text-2);
    margin: 0;
  }

  .desk {
    max-width: 940px;
    margin: 48px auto 0;
    padding: 0 24px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    align-items: stretch;
  }

  .ticket {
    border: 1px solid var(--border-strong);
    border-radius: 2px;
    background: var(--surface);
    font-family: var(--mono);
    display: flex;
    flex-direction: column;
  }

  .ticket--pro {
    border-color: var(--accent);
  }

  .t-head {
    display: flex;
    justify-content: space-between;
    padding: 12px 18px;
    border-bottom: 1px dashed var(--border-strong);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-2);
  }

  .ticket--pro .t-head {
    color: var(--accent);
  }

  .t-body {
    padding: 8px 18px 14px;
    flex: 1;
  }

  .t-row {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    padding: 8px 0;
    border-bottom: 1px solid var(--border);
    font-size: 12px;
    color: var(--text-2);
  }

  .t-row .k {
    color: var(--text-3);
  }

  .t-row .v {
    text-align: right;
    white-space: nowrap;
  }

  .t-row .v.up {
    color: var(--up);
  }

  .t-total {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 12px;
    padding: 14px 18px;
    font-size: 13px;
    border-top: 1px dashed var(--border-strong);
  }

  .t-price {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 6px;
  }

  .t-badge {
    display: inline-flex;
    align-items: center;
    padding: 2px 7px;
    border: 1px solid rgba(255, 176, 0, 0.45);
    background: rgba(255, 176, 0, 0.1);
    color: var(--accent);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .t-price-row {
    display: flex;
    align-items: baseline;
    gap: 8px;
  }

  .t-list,
  .t-list-inline {
    color: var(--text-3);
    font-size: 13px;
    text-decoration: line-through;
    text-decoration-thickness: 1px;
  }

  .t-list-inline {
    margin-right: 4px;
    font-size: 11px;
  }

  .t-total b {
    color: var(--accent);
    font-weight: 400;
    font-size: 22px;
  }

  .t-total small {
    font-size: 12px;
    color: var(--text-2);
  }

  .t-cta {
    padding: 0 18px 18px;
  }

  .t-cta .btn,
  .t-cta .btn-ghost {
    display: block;
    width: 100%;
    text-align: center;
    font-family: var(--mono);
    font-size: 13px;
  }

  .t-alt {
    text-align: center;
    font-size: 11px;
    color: var(--text-3);
    margin: 10px 0 0;
  }

  .t-alt a {
    color: var(--text-2);
  }

  .matrix {
    max-width: 880px;
    margin: 88px auto 0;
    padding: 0 24px;
  }

  .matrix .rule {
    margin-bottom: 28px;
  }

  .table-wrap {
    overflow-x: auto;
  }

  table {
    width: 100%;
    min-width: 560px;
    border-collapse: collapse;
    border: 1px solid var(--border-strong);
  }

  th,
  td {
    border: 1px solid var(--border);
    padding: 12px 16px;
    font-size: 13px;
    text-align: left;
  }

  thead th {
    background: var(--surface);
    font-family: var(--mono);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-2);
  }

  thead th.col {
    width: 15%;
    text-align: center;
  }

  thead th.pro {
    color: var(--accent);
  }

  .group td {
    background: var(--surface);
    font-family: var(--mono);
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--text-3);
    padding: 6px 16px;
  }

  td.cap {
    color: var(--text-2);
  }

  td.mark {
    font-family: var(--mono);
    text-align: center;
  }

  .yes {
    color: var(--up);
  }

  .no {
    color: var(--text-3);
  }

  td.mark.text {
    color: var(--text-2);
    font-size: 12px;
    white-space: nowrap;
  }

  .pro-col {
    background: rgba(255, 176, 0, 0.04);
  }

  .faq {
    max-width: 640px;
    margin: 88px auto 0;
    padding: 0 24px;
  }

  .faq .rule {
    margin-bottom: 24px;
  }

  .faq dt {
    font-weight: 600;
    margin-top: 24px;
    font-size: 15px;
  }

  .faq dd {
    color: var(--text-2);
    margin: 8px 0 0;
    line-height: 1.8;
    font-size: 14px;
  }

  @media (max-width: 720px) {
    .head h1 {
      font-size: 36px;
    }

    .desk {
      grid-template-columns: 1fr;
    }
  }
</style>
